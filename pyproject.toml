[project]
name = "bible-verse-tweets"
version = "0.2.0"
description = "Telegram bot that reads today's public mood and posts a matching Bible verse."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "vaderSentiment>=3.3",
    "python-dotenv>=1.0",
    "apscheduler>=3.10",
]

[tool.uv]
package = false
