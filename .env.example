# Create a bot with @BotFather on Telegram to get this token.
TELEGRAM_BOT_TOKEN=

# Chat/channel to post the daily verse to (used by `python main.py post`).
# Message your bot once, then run `python main.py bot` momentarily and check
# the console/getUpdates output for your chat id, or use @userinfobot.
TELEGRAM_CHAT_ID=
